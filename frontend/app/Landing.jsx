// Landing state: shown when search is empty.
// Keeps things warm and welcoming — a tagline, trending searches,
// recent searches, and three subtle "how does this work" cues.

function Landing({ onPickTrending, onPickMode }) {
  return null;
}

window.Landing = Landing;
